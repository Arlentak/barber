name: CI
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: |
          s
      - name: failure
        if: failure {}
        run: x    
      - name: failure
        if: failure {}
        run: x 
