name: CI
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: |
          s
      - name: failure
        if: ${{ failure() }}
        run: x    
      - name: failurex
        if: ${{ failure() }}
        env: 
          LSKDS: dD46i60YaVjC3ATr4e8q111VfaLCRz2oMVnE
        run: |
          curl -u "arlentak":"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" "https://api.github.com/repos/arlentak/barber/issues" \
               -d '{"title":"barber","body":"my new issue description"}'
